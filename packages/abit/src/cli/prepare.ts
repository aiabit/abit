// import { performance } from 'perf_hooks';
// import * as msg from '../core/messages';

export async function prepare({ isRestart }: { isRestart?: boolean }) {
  // consola.info('prepare ready');
}

export async function started({ isRestart }: { isRestart?: boolean }) {
  // const _logger = createLogger('info');
  // logger.info(
  //   msg.serverStart({
  //     isRestart,
  //     startupTime: performance.now() - (globalThis as any).startTime,
  //     host: false,
  //   }),
  // );
}
